/* ================================
   CSS VARIABLES
   ================================ */
:root {
  /* Colors - Gradients */
  /* Colors - Gradients */
  /* Colors - Gradients */
  --gradient-bg-dark: linear-gradient(to bottom right, hsl(220, 30%, 8%) 3%, hsl(220, 40%, 4%) 97%);
  --gradient-bg-light: linear-gradient(to bottom right, hsla(210, 50%, 15%, 0.3) 0%, hsla(220, 40%, 8%, 0) 100%), hsl(220, 35%, 10%);
  --gradient-accent-1: linear-gradient(to bottom right, hsl(210, 100%, 60%) 0%, hsla(210, 100%, 60%, 0) 50%);
  --gradient-accent-2: linear-gradient(135deg, hsla(210, 90%, 55%, 0.25) 0%, hsla(210, 90%, 55%, 0) 60%), hsl(220, 35%, 10%);
  --gradient-border: linear-gradient(to bottom right, hsl(210, 30%, 25%) 0%, hsla(210, 30%, 25%, 0) 50%);
  --gradient-text: linear-gradient(to right, hsl(210, 100%, 65%), hsl(200, 100%, 75%));

  /* Colors - Solid */
  --color-bg-primary: hsl(220, 40%, 5%);
  --color-bg-secondary: hsl(220, 35%, 8%);
  --color-bg-tertiary: hsl(220, 30%, 10%);
  --color-surface: hsl(220, 25%, 12%);
  --color-border: hsl(210, 20%, 20%);
  
  --color-text-primary: hsl(0, 0%, 100%);
  --color-text-secondary: hsl(210, 20%, 98%);
  --color-text-muted: hsl(210, 15%, 75%);
  --color-text-muted-70: hsla(210, 15%, 75%, 0.7);
  
  --color-accent-cyan: hsl(210, 100%, 60%);
  --color-accent-yellow: hsl(210, 95%, 55%);
  --color-accent-red: hsl(0, 70%, 55%);
  /* Typography */
  --font-family: 'Poppins', sans-serif;
  
  --font-size-h1: 24px;
  --font-size-h2: 18px;
  --font-size-h3: 17px;
  --font-size-h4: 16px;
  --font-size-base: 15px;
  --font-size-sm: 14px;
  --font-size-xs: 13px;
  --font-size-xxs: 11px;
  
  --font-weight-light: 300;
  --font-weight-regular: 400;
  --font-weight-medium: 500;
  --font-weight-semibold: 600;

  /* Shadows */
  --shadow-sm: -4px 8px 24px hsla(0, 0%, 0%, 0.25);
  --shadow-md: 0 16px 30px hsla(0, 0%, 0%, 0.25);
  --shadow-lg: 0 16px 40px hsla(0, 0%, 0%, 0.25);
  --shadow-xl: 0 25px 50px hsla(0, 0%, 0%, 0.15);
  --shadow-2xl: 0 24px 80px hsla(0, 0%, 0%, 0.25);

  /* Transitions */
  --transition-fast: 0.25s ease;
  --transition-normal: 0.5s ease-in-out;

  /* Spacing */
  --spacing-xs: 8px;
  --spacing-sm: 12px;
  --spacing-md: 16px;
  --spacing-lg: 24px;
  --spacing-xl: 32px;

  /* Border Radius */
  --radius-sm: 8px;
  --radius-md: 12px;
  --radius-lg: 14px;
  --radius-xl: 16px;
  --radius-2xl: 20px;
}

/* ================================
   RESET & BASE STYLES
   ================================ */
*, *::before, *::after {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html {
  font-family: var(--font-family);
  scroll-behavior: smooth;
}

body {
  background: var(--color-bg-primary);
  color: var(--color-text-secondary);
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

a {
  text-decoration: none;
  color: inherit;
}

ul, ol {
  list-style: none;
}

img {
  max-width: 100%;
  display: block;
}

button {
  font: inherit;
  background: none;
  border: none;
  cursor: pointer;
  color: inherit;
}

input, textarea {
  font: inherit;
  width: 100%;
  background: none;
  border: none;
  outline: none;
}

::selection {
  background: var(--color-accent-yellow);
  color: var(--color-bg-primary);
}

:focus-visible {
  outline: 2px solid var(--color-accent-cyan);
  outline-offset: 2px;
}

/* ================================
   TYPOGRAPHY
   ================================ */
.h2, .h3, .h4, .h5 {
  color: var(--color-text-secondary);
  text-transform: capitalize;
  font-weight: var(--font-weight-medium);
}

.h2 { font-size: var(--font-size-h1); }
.h3 { font-size: var(--font-size-h2); }
.h4 { font-size: var(--font-size-h4); }
.h5 { 
  font-size: var(--font-size-xs);
  font-weight: var(--font-weight-medium);
}

/* ================================
   LAYOUT
   ================================ */
main {
  margin: 15px 12px 75px;
  min-width: 259px;
}

.main-content {
  width: 100%;
}

/* ================================
   COMMON COMPONENTS
   ================================ */

/* Card Base */
.sidebar,
article {
  background: var(--color-bg-secondary);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-2xl);
  padding: 15px;
  box-shadow: var(--shadow-sm);
}

/* Separator */
.separator {
  width: 100%;
  height: 1px;
  background: var(--color-border);
  margin: var(--spacing-md) 0;
}

/* Icon Box */
.icon-box {
  position: relative;
  background: var(--gradient-border);
  width: 30px;
  height: 30px;
  border-radius: var(--radius-sm);
  display: flex;
  justify-content: center;
  align-items: center;
  font-size: 16px;
  color: var(--color-accent-yellow);
  box-shadow: var(--shadow-sm);
  z-index: 1;
}

.icon-box::before {
  content: "";
  position: absolute;
  inset: 1px;
  background: var(--color-bg-tertiary);
  border-radius: inherit;
  z-index: -1;
}

/* Article Title */
.article-title {
  position: relative;
  padding-bottom: 7px;
  margin-bottom: 15px;
}

.article-title::after {
  content: "";
  position: absolute;
  bottom: 0;
  left: 0;
  width: 30px;
  height: 3px;
  background: var(--gradient-text);
  border-radius: 3px;
}

/* Content Card */
.content-card {
  position: relative;
  background: var(--gradient-border);
  padding: 15px;
  padding-top: 45px;
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-md);
  z-index: 1;
}

.content-card::before {
  content: "";
  position: absolute;
  inset: 1px;
  background: var(--gradient-bg-light);
  border-radius: inherit;
  z-index: -1;
}

/* Scrollbar */
.has-scrollbar {
  overflow-x: auto;
  scroll-behavior: smooth;
  scroll-snap-type: inline mandatory;
}

.has-scrollbar::-webkit-scrollbar {
  width: 5px;
  height: 5px;
}

.has-scrollbar::-webkit-scrollbar-track {
  background: var(--color-surface);
  border-radius: 5px;
}

.has-scrollbar::-webkit-scrollbar-thumb {
  background: var(--color-accent-yellow);
  border-radius: 5px;
}

/* ================================
   SIDEBAR
   ================================ */
.sidebar {
  margin-bottom: 15px;
  max-height: 112px;
  overflow: hidden;
  transition: var(--transition-normal);
}

.sidebar.active {
  max-height: 405px;
}

/* Sidebar Info */
.sidebar-info {
  position: relative;
  display: flex;
  align-items: center;
  gap: 15px;
}

.avatar-box img {
  background: var(--gradient-bg-dark);
  border-radius: var(--radius-2xl);
  height: 80px;
  width: 80px;
  object-fit: cover;
}

.info-content .name {
  color: var(--color-text-secondary);
  font-size: var(--font-size-h3);
  font-weight: var(--font-weight-medium);
  margin-bottom: 10px;
}

.info-content .title {
  color: var(--color-text-primary);
  background: var(--color-surface);
  font-size: var(--font-size-xxs);
  font-weight: var(--font-weight-light);
  width: max-content;
  padding: 3px 12px;
  border-radius: var(--radius-sm);
}

/* Info More Button */
.info_more-btn {
  position: absolute;
  top: -15px;
  right: -15px;
  border-radius: 0 15px;
  font-size: 13px;
  color: var(--color-accent-yellow);
  background: var(--gradient-border);
  padding: 10px;
  box-shadow: var(--shadow-md);
  transition: var(--transition-fast);
  z-index: 1;
}

.info_more-btn::before {
  content: "";
  position: absolute;
  inset: 1px;
  border-radius: inherit;
  background: var(--gradient-bg-light);
  transition: var(--transition-fast);
  z-index: -1;
}

.info_more-btn:hover {
  background: var(--gradient-accent-1);
}

.info_more-btn:hover::before {
  background: var(--gradient-accent-2);
}

.info_more-btn span {
  display: none;
}

/* Sidebar Info More */
.sidebar-info_more {
  opacity: 0;
  visibility: hidden;
  transition: var(--transition-normal);
}

.sidebar.active .sidebar-info_more {
  opacity: 1;
  visibility: visible;
}

/* Contacts List */
.contacts-list {
  display: grid;
  grid-template-columns: 1fr;
  gap: var(--spacing-md);
}

.contact-item {
  display: flex;
  align-items: center;
  gap: var(--spacing-md);
}

.contact-info {
  flex: 1;
  min-width: 0;
}

.contact-title {
  color: var(--color-text-muted-70);
  font-size: var(--font-size-xxs);
  text-transform: uppercase;
  margin-bottom: 2px;
}

.contact-link,
.contact-info time,
.contact-info address {
  color: var(--color-text-secondary);
  font-size: var(--font-size-xs);
}

.contact-info address {
  font-style: normal;
}

.contact-link:hover {
  color: var(--color-accent-cyan);
}

/* Social List */
.social-list {
  display: flex;
  gap: 15px;
  padding: 4px 0 0 7px;
}

.social-link {
  color: var(--color-text-muted-70);
  font-size: 18px;
  transition: var(--transition-fast);
}

.social-link:hover {
  color: var(--color-text-muted);
}

/* ================================
   NAVBAR
   ================================ */
.navbar {
  position: fixed;
  bottom: 0;
  left: 0;
  width: 100%;
  background: hsla(240, 1%, 17%, 0.75);
  backdrop-filter: blur(10px);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md) var(--radius-md) 0 0;
  box-shadow: var(--shadow-md);
  z-index: 5;
}

.navbar-list {
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 0 10px;
}

.navbar-link {
  color: var(--color-text-muted);
  font-size: var(--font-size-xxs);
  padding: 20px 7px;
  transition: var(--transition-fast);
}

.navbar-link:hover {
  color: var(--color-text-muted-70);
}

.navbar-link.active {
  color: var(--color-accent-yellow);
}

/* ================================
   ARTICLE ANIMATIONS
   ================================ */
article {
  display: none;
  animation: fadeIn 0.5s ease;
}

article.active {
  display: block;
}

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(10px); }
  to { opacity: 1; transform: translateY(0); }
}

/* ================================
   ABOUT PAGE
   ================================ */

/* About Text */
.about-text {
  color: var(--color-text-muted);
  font-size: var(--font-size-sm);
  font-weight: var(--font-weight-light);
  line-height: 1.6;
  margin-bottom: var(--spacing-lg);
}

.about-text p + p {
  margin-top: 15px;
}

/* Service Section */
.service {
  margin-bottom: 35px;
}

.service-title {
  margin-bottom: 20px;
}

.service-list {
  display: grid;
  grid-template-columns: 1fr;
  gap: 20px;
}

.service-item {
  position: relative;
  background: var(--gradient-border);
  padding: 20px;
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-md);
  z-index: 1;
}

.service-item::before {
  content: "";
  position: absolute;
  inset: 1px;
  background: var(--gradient-bg-light);
  border-radius: inherit;
  z-index: -1;
}

.service-icon-box {
  margin-bottom: 10px;
}

.service-icon-box img {
  margin: 0 auto;
}

.service-content-box {
  text-align: center;
}

.service-item-title {
  margin-bottom: 7px;
}

.service-item-text {
  color: var(--color-text-muted);
  font-size: var(--font-size-sm);
  font-weight: var(--font-weight-light);
  line-height: 1.6;
}

/* Testimonials */
.testimonials {
  margin-bottom: 30px;
}

.testimonials-title {
  margin-bottom: 20px;
}

.testimonials-list {
  display: flex;
  gap: 15px;
  margin: 0 -15px;
  padding: 25px 15px 35px;
  overflow-x: auto;
  scroll-snap-type: inline mandatory;
}

.testimonials-item {
  min-width: 100%;
  scroll-snap-align: center;
}

.testimonials-avatar-box {
  position: absolute;
  top: 0;
  left: 0;
  transform: translate(15px, -25px);
  background: var(--gradient-bg-dark);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-sm);
}

.testimonials-item-title {
  margin-bottom: 7px;
}

.testimonials-text {
  color: var(--color-text-muted);
  font-size: var(--font-size-sm);
  font-weight: var(--font-weight-light);
  line-height: 1.6;
  display: -webkit-box;
  -webkit-line-clamp: 4;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

/* Clients */
.clients {
  margin-bottom: 15px;
}

.clients-list {
  display: flex;
  gap: 15px;
  margin: 0 -15px;
  padding: 25px;
  overflow-x: auto;
  scroll-snap-type: inline mandatory;
}

.clients-item {
  min-width: 50%;
  scroll-snap-align: start;
}

.clients-item img {
  width: 100%;
  filter: grayscale(1);
  transition: var(--transition-fast);
}

.clients-item img:hover {
  filter: grayscale(0);
}

/* ================================
   RESUME PAGE
   ================================ */

/* Timeline */
.timeline {
  margin-bottom: 30px;
}

.timeline .title-wrapper {
  display: flex;
  align-items: center;
  gap: 15px;
  margin-bottom: 25px;
}

.timeline-list {
  font-size: var(--font-size-sm);
  margin-left: 45px;
}

.timeline-item {
  position: relative;
}

.timeline-item + .timeline-item {
  margin-top: 20px;
}

.timeline-item-title {
  font-size: var(--font-size-sm);
  line-height: 1.3;
  margin-bottom: 7px;
}

.timeline-list span {
  color: var(--color-accent-cyan);
  font-weight: var(--font-weight-regular);
  line-height: 1.6;
}

.timeline-item:not(:last-child)::before {
  content: "";
  position: absolute;
  top: -25px;
  left: -30px;
  width: 1px;
  height: calc(100% + 50px);
  background: var(--color-border);
}

.timeline-item::after {
  content: "";
  position: absolute;
  top: 5px;
  left: -33px;
  height: 6px;
  width: 6px;
  background: var(--gradient-text);
  border-radius: 50%;
  box-shadow: 0 0 0 4px var(--color-border);
}

.timeline-text {
  color: var(--color-text-muted);
  font-weight: var(--font-weight-light);
  line-height: 1.6;
}

/* Skills */
.skills-title {
  margin-bottom: 20px;
}

.skills-list {
  padding: 20px;
}

.skills-item + .skills-item {
  margin-top: 15px;
}

.skill .title-wrapper {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 8px;
}

.skill .title-wrapper data {
  color: var(--color-text-muted);
  font-size: var(--font-size-xs);
  font-weight: var(--font-weight-light);
}

.skill-progress-bg {
  background: var(--color-border);
  width: 100%;
  height: 8px;
  border-radius: 10px;
  overflow: hidden;
}

.skill-progress-fill {
  background: var(--gradient-text);
  height: 100%;
  border-radius: inherit;
  transition: width 0.3s ease;
}

/* ================================
   PORTFOLIO PAGE
   ================================ */

/* Filter List */
.filter-list {
  display: none;
}

.filter-select-box {
  position: relative;
  margin-bottom: 25px;
}

.filter-select {
  background: var(--color-bg-secondary);
  color: var(--color-text-muted);
  display: flex;
  justify-content: space-between;
  align-items: center;
  width: 100%;
  padding: 12px 16px;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  font-size: var(--font-size-sm);
}

/* Project List */
.project-list {
  display: grid;
  grid-template-columns: 1fr;
  gap: 30px;
  margin-bottom: 10px;
}

.project-item {
  display: none;
}

.project-item.active {
  display: block;
  animation: scaleUp 0.25s ease;
}

@keyframes scaleUp {
  from { transform: scale(0.9); opacity: 0; }
  to { transform: scale(1); opacity: 1; }
}

.project-item > a {
  display: block;
}

.project-img {
  position: relative;
  width: 100%;
  height: 200px;
  border-radius: var(--radius-xl);
  overflow: hidden;
  margin-bottom: 15px;
}

.project-img img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: var(--transition-fast);
}

.project-item > a:hover .project-img img {
  transform: scale(1.1);
}

.project-img::before {
  content: "";
  position: absolute;
  inset: 0;
  background: transparent;
  z-index: 1;
  transition: var(--transition-fast);
}

.project-item > a:hover .project-img::before {
  background: hsla(0, 0%, 0%, 0.5);
}

.project-item-icon-box {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%) scale(0.8);
  background: var(--color-border);
  color: var(--color-accent-yellow);
  font-size: 20px;
  padding: 18px;
  border-radius: var(--radius-md);
  opacity: 0;
  z-index: 2;
  transition: var(--transition-fast);
}

.project-item > a:hover .project-item-icon-box {
  transform: translate(-50%, -50%) scale(1);
  opacity: 1;
}

.project-title,
.project-category {
  margin-left: 10px;
}

.project-title {
  color: var(--color-text-secondary);
  font-size: var(--font-size-base);
  font-weight: var(--font-weight-regular);
  text-transform: capitalize;
  line-height: 1.3;
}

.project-category {
  color: var(--color-text-muted-70);
  font-size: var(--font-size-sm);
  font-weight: var(--font-weight-light);
}

/* ================================
   BLOG PAGE
   ================================ */
.blog-posts {
  margin-bottom: 10px;
}

.blog-posts-list {
  display: grid;
  grid-template-columns: 1fr;
  gap: 20px;
}

.blog-post-item > a {
  position: relative;
  background: var(--gradient-border);
  display: block;
  height: 100%;
  box-shadow: var(--shadow-xl);
  border-radius: var(--radius-xl);
  overflow: hidden;
  z-index: 1;
}

.blog-post-item > a::before {
  content: "";
  position: absolute;
  inset: 1px;
  border-radius: inherit;
  background: var(--color-bg-tertiary);
  z-index: -1;
}

.blog-banner-box {
  width: 100%;
  height: 200px;
  overflow: hidden;
}

.blog-banner-box img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: var(--transition-fast);
}

.blog-post-item > a:hover .blog-banner-box img {
  transform: scale(1.1);
}

.blog-content {
  padding: 15px;
}

.blog-meta {
  display: flex;
  align-items: center;
  gap: 7px;
  margin-bottom: 10px;
}

.blog-category,
.blog-meta time {
  color: var(--color-text-muted-70);
  font-size: var(--font-size-sm);
  font-weight: var(--font-weight-light);
}

.blog-meta .dot {
  background: var(--color-text-muted-70);
  width: 4px;
  height: 4px;
  border-radius: 50%;
}

.blog-item-title {
  margin-bottom: 10px;
  line-height: 1.3;
  transition: var(--transition-fast);
}

.blog-post-item > a:hover .blog-item-title {
  color: var(--color-accent-yellow);
}

.blog-text {
  color: var(--color-text-muted);
  font-size: var(--font-size-sm);
  font-weight: var(--font-weight-light);
  line-height: 1.6;
}

/* ================================
   CONTACT PAGE
   ================================ */

/* Map */
.mapbox {
  position: relative;
  height: 250px;
  width: 100%;
  border-radius: var(--radius-xl);
  margin-bottom: 30px;
  border: 1px solid var(--color-border);
  overflow: hidden;
}

.mapbox figure {
  height: 100%;
}

.mapbox iframe {
  width: 100%;
  height: 100%;
  border: none;
  filter: grayscale(1) invert(1);
}

/* Contact Form */
.contact-form {
  margin-bottom: 10px;
}

.form-title {
  margin-bottom: 20px;
}

.input-wrapper {
  display: grid;
  grid-template-columns: 1fr;
  gap: 25px;
  margin-bottom: 25px;
}

.form-input {
  color: var(--color-text-secondary);
  font-size: var(--font-size-sm);
  padding: 13px 20px;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  background: transparent;
  transition: var(--transition-fast);
}

.form-input::placeholder {
  color: var(--color-text-muted);
  font-weight: var(--font-weight-medium);
}

.form-input:focus {
  border-color: var(--color-accent-yellow);
}

.form-input:focus:invalid {
  border-color: var(--color-accent-red);
}

textarea.form-input {
  min-height: 100px;
  height: 120px;
  max-height: 200px;
  resize: vertical;
  margin-bottom: 25px;
}

textarea.form-input::-webkit-resizer {
  display: none;
}

/* Form Button */
.form-btn {
  position: relative;
  width: 100%;
  background: var(--gradient-border);
  color: var(--color-accent-yellow);
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 10px;
  padding: 13px 20px;
  border-radius: var(--radius-lg);
  font-size: var(--font-size-sm);
  text-transform: capitalize;
  box-shadow: var(--shadow-lg);
  z-index: 1;
  transition: var(--transition-fast);
}

.form-btn::before {
  content: "";
  position: absolute;
  inset: 1px;
  background: var(--gradient-bg-light);
  border-radius: inherit;
  z-index: -1;
  transition: var(--transition-fast);
}

.form-btn:hover {
  background: var(--gradient-accent-1);
}

.form-btn:hover::before {
  background: var(--gradient-accent-2);
}

.form-btn:disabled {
  opacity: 0.7;
  cursor: not-allowed;
}

.form-btn:disabled:hover {
  background: var(--gradient-border);
}

.form-btn:disabled:hover::before {
  background: var(--gradient-bg-light);
}

/* ================================
   RESPONSIVE - TABLET
   ================================ */
@media (min-width: 580px) {
  :root {
    --font-size-h1: 32px;
    --font-size-h2: 24px;
    --font-size-h3: 26px;
    --font-size-h4: 18px;
    --font-size-sm: 15px;
    --font-size-xs: 15px;
    --font-size-xxs: 12px;
  }

  .sidebar, article {
    width: 520px;
    margin-inline: auto;
    padding: 30px;
  }

  main {
    margin-top: 60px;
    margin-bottom: 100px;
  }

  .sidebar {
    max-height: 180px;
    margin-bottom: 30px;
  }

  .sidebar.active {
    max-height: 584px;
  }

  .sidebar-info {
    gap: 25px;
  }

  .avatar-box {
    border-radius: 30px;
  }

  .avatar-box img {
    width: 120px;
    height: 120px;
  }

  .info-content .name {
    margin-bottom: 15px;
  }

  .info-content .title {
    padding: 5px 18px;
  }

  .info_more-btn {
    top: -30px;
    right: -30px;
    padding: 10px 15px;
  }

  .info_more-btn span {
    display: block;
    font-size: var(--font-size-xxs);
  }

  .info_more-btn ion-icon {
    display: none;
  }

  .separator {
    margin: 32px 0;
  }

  .contacts-list {
    gap: 20px;
  }

  .icon-box {
    width: 48px;
    height: 48px;
    border-radius: var(--radius-md);
    font-size: 18px;
  }

  .article-title {
    font-weight: var(--font-weight-semibold);
    padding-bottom: 15px;
  }

  .article-title::after {
    width: 40px;
    height: 5px;
  }

  /* Navbar */
  .navbar {
    border-radius: var(--radius-2xl) var(--radius-2xl) 0 0;
  }

  .navbar-list {
    gap: 20px;
  }

  .navbar-link {
    font-size: 14px;
  }

  /* About */
  .about-text {
    margin-bottom: 40px;
  }

  .service-item {
    display: flex;
    align-items: flex-start;
    gap: 18px;
    padding: 30px;
  }

  .service-icon-box {
    margin-bottom: 0;
    margin-top: 5px;
  }

  .service-content-box {
    text-align: left;
  }

  .testimonials-title {
    margin-bottom: 25px;
  }

  .testimonials-list {
    gap: 30px;
    margin: 0 -30px;
    padding: 30px 30px 35px;
  }

  .content-card {
    padding: 30px 30px 25px;
  }

  .testimonials-avatar-box {
    transform: translate(30px, -30px);
    border-radius: var(--radius-2xl);
  }

  .testimonials-avatar-box img {
    width: 80px;
  }

  .testimonials-item-title {
    margin-bottom: 10px;
    margin-left: 95px;
  }

  .testimonials-text {
    -webkit-line-clamp: 2;
  }

  .clients-list {
    gap: 50px;
    margin: 0 -30px;
    padding: 45px;
  }

  .clients-item {
    min-width: calc(33.33% - 35px);
  }

  /* Resume */
  .timeline-list {
    margin-left: 65px;
  }

  .timeline-item:not(:last-child)::before {
    left: -40px;
  }

  .timeline-item::after {
    height: 8px;
    width: 8px;
    left: -43px;
  }

  .skills-item + .skills-item {
    margin-top: 25px;
  }

  /* Portfolio & Blog */
  .project-img,
  .blog-banner-box {
    border-radius: var(--radius-xl);
    height: auto;
  }

  .blog-posts-list {
    gap: 30px;
  }

  .blog-content {
    padding: 25px;
  }

  /* Contact */
  .mapbox {
    height: 380px;
    border-radius: 18px;
  }

  .input-wrapper {
    gap: 30px;
    margin-bottom: 30px;
  }

  .form-input {
    padding: 15px 20px;
  }

  textarea.form-input {
    margin-bottom: 30px;
  }

  .form-btn {
    font-size: var(--font-size-sm);
    padding: 16px 20px;
  }
}

/* ================================
   RESPONSIVE - DESKTOP (768px+)
   ================================ */
@media (min-width: 768px) {
  .sidebar, article {
    width: 700px;
  }

  .has-scrollbar::-webkit-scrollbar-button {
    width: 100px;
  }

  /* Sidebar */
  .contacts-list {
    grid-template-columns: 1fr 1fr;
    gap: 30px 15px;
  }

  /* Navbar */
  .navbar-link {
    font-size: 15px;
  }

  /* Portfolio */
  .filter-list {
    display: flex;
    justify-content: flex-start;
    align-items: center;
    gap: 25px;
    padding-left: 5px;
    margin-bottom: 30px;
  }

  .filter-select-box {
    display: none;
  }

  .filter-item button {
    color: var(--color-text-muted);
    font-size: var(--font-size-base);
    transition: var(--transition-fast);
  }

  .filter-item button:hover {
    color: var(--color-text-muted-70);
  }

  .filter-item button.active {
    color: var(--color-accent-yellow);
  }

  .project-list,
  .blog-posts-list {
    grid-template-columns: 1fr 1fr;
  }

  /* Contact */
  .input-wrapper {
    grid-template-columns: 1fr 1fr;
  }

  .form-btn {
    width: max-content;
    margin-left: auto;
  }
}

/* ================================
   RESPONSIVE - LARGE DESKTOP (1024px+)
   ================================ */
@media (min-width: 1024px) {
  :root {
    --shadow-sm: -4px 8px 24px hsla(0, 0%, 0%, 0.125);
    --shadow-md: 0 16px 30px hsla(0, 0%, 0%, 0.125);
    --shadow-lg: 0 16px 40px hsla(0, 0%, 0%, 0.125);
  }

  .sidebar, article {
    width: 950px;
    box-shadow: var(--shadow-2xl);
  }

  main {
    margin-bottom: 60px;
  }

  .main-content {
    position: relative;
    width: max-content;
    margin: auto;
  }

  /* Navbar */
  .navbar {
    position: absolute;
    bottom: auto;
    top: 0;
    left: auto;
    right: 0;
    width: max-content;
    border-radius: 0 var(--radius-2xl);
    padding: 0 20px;
    box-shadow: none;
  }

  .navbar-list {
    gap: 30px;
    padding: 0 20px;
  }

  .navbar-link {
    font-weight: var(--font-weight-medium);
  }

  /* About */
  .service-list {
    grid-template-columns: 1fr 1fr;
    gap: 20px 25px;
  }

  .testimonials-item {
    min-width: calc(50% - 15px);
  }

  .clients-item {
    min-width: calc(25% - 38px);
  }

  /* Portfolio */
  .project-list {
    grid-template-columns: repeat(3, 1fr);
  }

  /* Blog */
  .blog-banner-box {
    height: 230px;
  }
}

/* ================================
   RESPONSIVE - EXTRA LARGE (1250px+)
   ================================ */
@media (min-width: 1250px) {
  body::-webkit-scrollbar {
    width: 20px;
  }

  body::-webkit-scrollbar-track {
    background: var(--color-bg-primary);
  }

  body::-webkit-scrollbar-thumb {
    border: 5px solid var(--color-bg-primary);
    background: hsla(0, 0%, 100%, 0.1);
    border-radius: 20px;
  }

  body::-webkit-scrollbar-thumb:hover {
    background: hsla(0, 0%, 100%, 0.15);
  }

  body::-webkit-scrollbar-button {
    height: 60px;
  }

  .sidebar, article {
    width: auto;
  }

  article {
    min-height: 100%;
  }

  main {
    max-width: 1200px;
    margin-inline: auto;
    display: flex;
    justify-content: center;
    align-items: stretch;
    gap: 25px;
  }

  .main-content {
    min-width: 75%;
    width: 75%;
    margin: 0;
  }

  /* Sidebar */
  .sidebar {
    position: sticky;
    top: 60px;
    max-height: max-content;
    height: 100%;
    margin-bottom: 0;
    padding-top: 60px;
    z-index: 1;
  }

  .sidebar-info {
    flex-direction: column;
  }

  .avatar-box img {
    width: 150px;
    height: 150px;
  }

  .info-content .name {
    white-space: nowrap;
    text-align: center;
  }

  .info-content .title {
    margin: auto;
  }

  .info_more-btn {
    display: none;
  }

  .sidebar-info_more {
    opacity: 1;
    visibility: visible;
  }

  .contacts-list {
    grid-template-columns: 1fr;
  }

  .contact-link {
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .contact-info :is(.contact-link, time, address) {
    font-size: 14px;
    font-weight: var(--font-weight-light);
  }

  .separator:last-of-type {
    margin: 15px 0;
    opacity: 0;
  }

  .social-list {
    justify-content: center;
  }

  /* Resume */
  .timeline-text {
    max-width: 700px;
  }
}

/* ================================
   UTILITY CLASSES
   ================================ */
.text-gradient {
  background: var(--gradient-text);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.fade-in {
  animation: fadeIn 0.5s ease;
}

.scale-up {
  animation: scaleUp 0.25s ease;
}

/* Screen reader only */
.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border-width: 0;
}